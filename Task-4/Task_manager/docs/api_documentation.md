# Task Management REST API

A simple Task Management REST API built using Go and the Gin framework.

The application uses an in-memory map for storing tasks.

## Run the Application

From the `task_manager` directory:

```bash
go run .